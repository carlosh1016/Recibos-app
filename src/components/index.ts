// Barrel de componentes de UI compartidos, para importar como
// `import { Button, Screen } from '../components'`. A diferencia de types/,
// aquí SÍ hay valores en runtime (componentes), así que es un `export` normal.
export { Button } from './Button'
export { Card } from './Card'
export { Field } from './Field'
export { ProgressBar } from './ProgressBar'
export { Screen } from './Screen'
export { StatusBadge } from './StatusBadge'
export { ESTILO_ESTADO } from './estadoRecibo'
