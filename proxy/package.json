{
  "name": "recibos-proxy",
  "private": true,
  "version": "0.0.0",
  "description": "Proxy serverless (Cloudflare Worker) que reenvía la imagen de la tirilla a Gemini y devuelve JSON estructurado. Guarda la clave de Gemini como secreto.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "~5.6.0",
    "wrangler": "^3.86.0"
  }
}
